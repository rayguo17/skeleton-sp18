import java.util.Objects;

/** An Integer tester created by Flik Enterprises. */
public class Flik {
    public static boolean isSameNumber(Integer a, Integer b) {
        return a==b;
        //.equals for object(Integer) autoboxing
        // == for primitives(int)
    }
}
